# E-Commerce Frontend

React + Vite + Axios frontend for the Spring Boot e-commerce backend.

## Run

```bash
npm install
npm run dev
```

Open `http://localhost:5173`.

Create `.env` from `.env.example` if the backend is not running at the default URL.
